import { TaskBarWrapper } from "~/components/TaskBarWrapper";
import { Screen } from "./styles";

export const Desktop = () => {
  return (
    <Screen>
      <TaskBarWrapper />
    </Screen>
  );
};
